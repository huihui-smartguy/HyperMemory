# HyperMemory 企业级记忆系统

## 前端面板需求设计文档 (PRD v1.0)

### 1. 产品概述

HyperMemory 定位于企业级智能体自主记忆中枢，旨在支撑百万级并发下的“搜推问一体化”业务链路。相较于传统仅提供增删改查的记忆库，HyperMemory 前端面板的核心使命是**将黑盒的 AI 自主进化（建模、提取、沉淀、推理）转化为白盒的可视化资产**。

本面板以极致简约的视觉体验，服务于 AI 算法架构师、业务运营专家及研发工程师，助力其对高维度的用户意图（特别是针对 TPO 推理期偏好的动态漂移）进行精准观测与干预。

---

### 2. UI/UX 设计规范 (Apple 极简架构)

界面设计严格遵循 Apple 官网的极致留白与沉浸式交互哲学，剥离传统 B 端 SaaS 面板的繁冗感。

* **色彩体系：** 全局背景采用纯净灰白（#F5F5F7）或深邃黑（暗黑模式 #000000）。摒弃大面积色块，通过极浅的毛玻璃阴影（Backdrop-filter）与 1px 细灰线分割区域。
* **字体排印：** 采用系统级无衬线字体（San Francisco / PingFang SC）。通过字重（Light/Regular/Semibold）而非颜色来构建信息层级。
* **全局 Mega Menu 导航：** 摒弃常驻的左侧边栏，采用顶部悬浮式超级导航。
* 默认状态仅展示文字：`HyperMemory`（Logo） | `基础面板` | `进阶面板` | `开发者中心`。
* 当鼠标悬停于 `基础面板` 或 `进阶面板` 时，下方平滑展开全宽度的毛玻璃质感二级菜单，展示图标、栏目名称及一句话说明。鼠标移出后平滑收起。



---

### 3. 基础面板设计 (稳态业务区)

对标顶级商业化记忆平台，提供高密度、低延迟的数据台账与运维监控。

#### 3.1 记忆金库 (Memory Vault)

* **核心功能：** 结构化展示系统摄入的所有原始记忆（raw.md）及格式化结果。
* **交互设计：**
* 顶部为一个类似 macOS Spotlight 的全局无边框搜索栏。
* 主体为沉浸式数据网格（Data Grid）。隐藏纵向边框，仅保留微弱横线。
* **实体胶囊：** 用户标签、偏好、触发场景（如：对隐私条款敏感、拒绝高风险资产）等元数据采用圆润的浅色小胶囊展示。


* **高级筛选：** 支持按 `Session_ID`、`Agent_ID` 以及时间跨度进行精准下钻。

#### 3.2 运行大盘 (Analytics)

* **核心功能：** 监控大盘与系统资源利用率。
* **指标呈现：**
* 提供 API 调用吞吐量、Go 网关路由延迟的核心折线图。
* 记忆分类饼图（事实记忆 vs 语义记忆 vs 画像规则）。
* 存储节点健康度与 TTL（生命周期）回收统计。



---

### 4. 进阶面板设计 (敏态进化区)

体现 HyperMemory 核心竞争力的“进化观测站”，将底层 Python 引擎的认知链路完全白盒化。

#### 4.1 Schema 进化车间 (Evolution Factory)

* **核心功能：** 展示系统基于惊喜度计算后触发的 Schema 优化建议，并完成人机协同审批。
* **界面布局：** 极简左右分栏。
* **左栏（认知树）：** 树状展示当前激活的 `Skill记忆Schema` 与 `通用记忆Schema`。
* **右栏（辩证视窗）：** 采用 Monaco Editor 引擎，呈现 Markdown 代码级 Diff 视图（新旧对比）。


* **流式交互 (SSE)：** 当底层正在进行“反事实推演”时，界面顶部显示打字机形态的推理流（例如：“检测到用户连续拒绝固定策略，推测风险偏好发生漂移，触发 TPO 规则重组...”）。
* **审批流：** Diff 视图底部悬浮微圆角卡片，包含 `采纳 (Accept)` 和 `驳回并修正 (Refine)` 按钮，完成经验沉淀闭环。

#### 4.2 认知拓扑引擎 (Cognitive Graph)

* **核心功能：** 将记忆从摄入到图谱编译的全生命周期可视化。
* **管道泳道：** 利用节点连线展示 `原始请求 -> 脱敏去重 -> 滑动窗口提取 -> 图谱编译` 的流转状态。
* **因果图谱编辑器：** 展示实体关联图谱。对于底层生成的“反事实提取”链路，采用特定的虚线样式高亮。允许架构师通过拖拽节点进行人工干预与微调。

#### 4.3 召回 X 光机 (Retrieval X-Ray)

* **核心功能：** 还原单次 Agent 请求背后，自适应单/多路召回的黑盒逻辑。
* **溯源瀑布流：**
* 横向时间轴展示该请求在 Go 网关、Milvus 向量库（并发路由）、关系型数据库中的耗时拆解。
* 点击任意耗时柱状图，侧滑展出对应的 JSON 日志，包括重排序（Rerank）得分明细和去重拦截记录，帮助排查 Bad Case。



---

### 5. 前后端对接与工程规范

为支撑前端交互的流畅度，BFF (Backend for Frontend) 与底层异构微服务的数据流须遵循严格规范。

| 模块区域 | 通信协议 | 渲染引擎/组件库 | 对接要求 |
| --- | --- | --- | --- |
| **Mega Menu 导航** | 静态加载 | React Server Components | 路由配置化，前端预渲染保障首屏极速加载。 |
| **基础面板数据表** | RESTful API | Ant Design (高定极简主题) | Go 网关提供分页接口，Node.js 侧实现 Redis 缓存，应对高频刷新。 |
| **Schema Diff 视窗** | SSE (流式推流) | Monaco Editor (React版) | Python 服务推送 `REASONING_CHUNK` 及 Markdown 文本，前端仅做 Diff 高亮。 |
| **拓扑与瀑布流** | RESTful API | AntV G6 (图谱) / ECharts | Go 必须在请求入口注入全局 `Trace_ID`，聚合全链路日志供前端树状解析。 |

### 6. 非功能性需求

* **海量节点渲染：** 认知图谱在面对超过 5000 个实体节点时，前端必须自动降级为“聚合簇（Cluster）”模式，保证浏览器帧率稳定在 60fps。
* **状态管理：** 使用 Zustand 维持全局的 Agent 会话与租户上下文，避免多级组件传参导致的渲染阻塞。
* **无缝降级：** 若 SSE 流式连接异常中断，前端需自动回退至 RESTful 长轮询，确保业务逻辑闭环不断联。
