# HyperMemory 企业级记忆系统

## 业务架构设计文档 (Business Architecture v1.0)

### 1. 战略定位与业务价值

**HyperMemory** 是一款专为大模型智能体（Agent）打造的企业级“元认知”记忆中枢，旨在彻底改变传统记忆系统“只存不思、被动调用”的弊端。

本系统以“搜推问一体化（Search-Recommend-Ask Integration）”**为核心业务骨架，深度融合金融科技领域对合规性与用户偏好的极高要求。系统将 TPO（Time-of-inference Preference Optimization，推理期偏好优化）作为 P0 级核心组件，结合 DPO（Direct Preference Optimization，直接偏好优化），构建**双轨认知引擎。它不仅为企业提供千人千面的长效个性化陪伴，更通过“自主记忆维护”实现企业业务经验（Skill）与工具调用法则的系统级沉淀。

### 2. 核心业务驱动力 (Business Drivers)

* **对抗偏好漂移 (Preference Drift)：** 实时捕获用户在金融交易、内容消费中的动态意图变化，通过 TPO 引擎在推理期毫秒级阻断错误推荐。
* **沉淀企业级认知资产 (Cognitive Asset Management)：** 将高优业务经验、合规约束、复杂操作SOP，通过“Schema 进化车间”转化为机器可读的持久化心智模型。
* **安全与合规防御 (Dual-Track Compliance)：** 事前 TPO 预判干预与事后 DPO 价值观对齐，确保金融级交互（如基金推荐、风险提示）的绝对安全。

---

### 3. 总体业务架构 (The 4-Layer Architecture)

业务架构自上而下划分为四大核心层级，实现从“端侧调度”到“深层认知”的完全解耦。

#### 3.1 接入与调度层 (Access & Dispatch Layer)

负责处理海量高并发请求，是系统的流量门户与意图分拣中心。

* **全局消息总线：** 接收来自前台 Agent / Copilot 的多模态请求。
* **意图探针与路由决策：** 毫秒级识别请求是属于“即时问答（Ask）”、“主动搜索（Search）”还是“意图推荐（Recommend）”，并智能调度至下层执行池。
* **多租户与 IAM 管控：** 面向企业级 SaaS 提供项目/环境隔离，确保 B 端不同业务线（如信贷部、理财部）的记忆数据物理/逻辑隔离。

#### 3.2 搜推问引擎层 (Core Processing Layer)

HyperMemory 的心脏，承担请求周期的三大实时任务：

* **任务一：记忆建模 (Memory Modeling)**
* 调用核心 Skill，通过 LLM 提取器从对话上下文（Frontmatter）中提取触发场景。
* 从功能范围提取“数据模型”，从操作步骤提取“业务流程”，从实现说明提取“规则约束”。
* 生成结构化产物：`Skill记忆Schema`（负责业务逻辑）与 `通用记忆Schema`（负责画像基座）。


* **任务二：记忆写入 (Memory Ingestion)**
* **合规脱敏管道：** 对涉及隐私的 PII 数据进行掩码处理。
* **去重与格式化：** 将处理后的数据标准化并写入 `raw.md` 原生记忆池。


* **任务三：记忆找回 (Adaptive Retrieval)**
* **自适应多路路由：** 根据意图复杂度，动态决定调用向量检索（语义）还是关系型检索（精准画像），或两者并发。
* **TPO 权重干预：** 在召回阶段，TPO 引擎实时介入，对历史记忆进行环境权重打分（如：用户当前处于“避险”情绪，历史“高风险偏好”记忆被动态降权）。
* **重排序 (Rerank) 与去重：** 组装最终的最优上下文注入 Agent Prompt。



#### 3.3 自主维护与进化层 (Autonomous Evolution Layer)

本系统的“潜意识”后台，异步运行，实现系统的自我迭代，不阻塞实时交互。

* **滑动窗口提取器：** 后台持续从海量 `raw.md` 中提纯事实记忆，同步进行情景、画像与语义的多维提取。
* **图谱编译与反事实推演：**
* 将离散记忆组装为金融知识实体与用户偏好图谱。
* **反事实因果提取：** 针对 Bad Case（如推荐被拒），推理“如果当时不这么推荐会怎样”，提炼底层逻辑。


* **Schema 优化与经验沉淀：**
* 核心算法：**惊喜度计算 (Surprise Detection)**。当用户行为偏离现有 Schema 预期（高惊喜度）时，触发**辩证推理**。
* 输出 `Schema 优化建议`，提交流水线，经前台“进化车间”人工审批后，完成系统认知的升级。



#### 3.4 基础设施与数据基座 (Infrastructure Layer)

* **向量记忆库：** 部署 Milvus (v2.6.8) 集群，承载千万级高维语义向量检索。
* **结构化画像库：** PostgreSQL / MySQL 集群，存储强类型的 Schema、租户配置与静态画像（Age, Risk Level 等）。
* **链路态势感知 (Traceability)：** 全局 Trace ID 贯穿 Go 网关与 Python 计算节点，捕获意图识别、召回耗时、TPO 干预日志，上报至前端“X 光机”大盘。

---

### 4. 关键业务流转 (Core Business Flows)

#### 4.1 搜推问一体化处理流 (实时交互)

1. **输入：** 用户提问“我最近想买点稳健的理财”。
2. **调度：** 消息总线识别为“推荐+问答”混合意图，下发至双轨引擎。
3. **找回：** 多路召回触发，抽取通用 Schema（用户 35 岁，净资产高）和历史事实记忆（上周拒绝了股票基金）。
4. **TPO 干预：** TPO 引擎判定当前存在“避险”偏好漂移，修改推荐策略权重。
5. **输出：** Agent 结合组装好的上下文，精准输出固收类理财推荐方案。

#### 4.2 记忆自主进化流 (异步沉淀)

1. **触发：** 记忆摄入器通过滑动窗口发现，近期大量高净值用户在周末对特定类别的基金推荐点了“不感兴趣”。
2. **计算：** 惊喜度模块打分超过阈值（0.85），触发反事实提取机制。
3. **推理：** 系统辩证推演得出结论：周末场景下，该类用户更倾向于低打扰的账户体检报告，而非直接的基金推销。
4. **优化：** 生成新的业务规则约束：“周末时段，针对高净值用户，冻结激进推荐 Skill，切换至资产盘点 Skill”。
5. **落地：** 向管理员的 `Schema 进化车间` 推送 Diff 代码建议，经由运营在前端面板点击“采纳”，全新 `Skill记忆Schema.md` 实时生效。
